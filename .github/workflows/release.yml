name: release

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  lint:
    uses: platform-mesh/.github/.github/workflows/job-golang-lint.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    with:
      useTask: true

  test:
    uses: platform-mesh/.github/.github/workflows/job-golang-test-source.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    secrets: inherit
    with:
      useTask: true
      useLocalCoverageConfig: true

  create-version:
    uses: platform-mesh/.github/.github/workflows/job-create-version.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    secrets: inherit
    permissions:
      contents: write

  docker-build-push:
    needs: [create-version, lint, test]
    uses: platform-mesh/.github/.github/workflows/job-docker-build-push.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    secrets: inherit
    permissions:
      contents: write
      packages: write
      id-token: write
    with:
      imageTagName: ghcr.io/platform-mesh/security-operator
      version: ${{ needs.create-version.outputs.version }}
      multiarch: true

  update-version:
    needs: [create-version, docker-build-push]
    uses: platform-mesh/.github/.github/workflows/job-chart-version-update.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    secrets: inherit
    with:
      appVersion: ${{ needs.create-version.outputs.version }}
      chart: security-operator
      targetRepository: platform-mesh/helm-charts

  sbom:
    needs: [create-version, docker-build-push]
    uses: platform-mesh/.github/.github/workflows/job-sbom.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    permissions:
      contents: read
      packages: read
    with:
      imageReference: ghcr.io/platform-mesh/security-operator:${{ needs.create-version.outputs.version }}

  image-ocm:
    needs: [create-version, docker-build-push, sbom]
    uses: platform-mesh/.github/.github/workflows/job-image-ocm.yml@a92948421e9344929f5b9edd6048aa688c1b8ace # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      imageReference: ghcr.io/platform-mesh/security-operator:${{ needs.create-version.outputs.version }}
      appVersion: ${{ needs.create-version.outputs.version }}
      repoName: security-operator
      commit: ${{ github.sha }}
